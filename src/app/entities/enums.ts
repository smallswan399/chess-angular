export enum TurnMove {
    WhiteMove,
    BlackMove
}

export enum Color {
    White,
    Black
}
export enum Timing {
    Game5Minutes,
    Game10Minutes,
    Game15Minutes
}

export enum GameResult {
    Draw, WhiteWin, BlackWin
}
